$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
$computers = Get-Content -Path "C:\computers.txt"  
$results = @()  
foreach ($computer in $computers) {  
    try {  
        $status = Test-Connection -ComputerName $computer -Count 2 -ErrorAction Stop  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Online"}  
    } catch {  
        $results += [PSCustomObject]@{ComputerName = $computer; Status = "Offline"}  
    }  
}
